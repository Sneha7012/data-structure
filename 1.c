#include<stdio.h>
#include<stdlib.h>
void main()
{
int l,b,a,p;
printf("enter length and breath of a rectangle:");
scanf("%d%d",&l,&b);
a=l*b;
p=2*(l+b);
printf("Area and Perimeter of rectangle is %d %d ",a,p);

}

